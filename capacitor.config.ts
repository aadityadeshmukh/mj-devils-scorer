import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hermes.cricketscoring',
  appName: 'Hermes Scorer',
  webDir: 'dist'
};

export default config;
